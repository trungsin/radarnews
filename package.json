{
  "name": "radarnews-pipeline",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "migrate:local": "wrangler d1 migrations apply radarnews --local"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.34.0"
  }
}
